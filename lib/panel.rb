module NewsGrafana
  class Panel

  def initialize (datasource, id, span, targets, title)
    @datasource = datasource,
    @id = id,
    @span = span,
    @targets = span,
    @title = title
  end

end

